# Locations

##### Where things are stored and what they do

- **local database for packages:** /usr/ports/db

- **list of files installed by each package:** /usr/ports/db/{PACKAGE}.files

- **list of installed packages;** /usr/ports/db/installed
